/// Temporary UI gate for WhatsApp automation controls.
///
/// Keep domain/data paths intact; flip to `true` when automation ships.
abstract final class WhatsAppAutomationUi {
  static const showAutomaticControls = false;

  /// Meta template approval actions (submit / sync / badge).
  static const showMetaApprovalUi = false;
}
